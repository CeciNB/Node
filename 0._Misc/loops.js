
const failures = ["mislick", "falling down", "skateboard accident", "tiktok fail"];

const failuresCopy = failures.map((failure) => {
    return failure
})

console.log(failuresCopy)

//const failuresCopy2 = failures.map((f,i) => console.log(f,i));
/*
const newFailures = failures.map((failure, index) => {​​
    console.log(failure, index);
}​​);*/

//console.log(newFailures)

/*
numbers.forEach((element) => {
  element < 0 || element > 6
    ? console.log(element)
    : discardedNumbers.push(element)
})
*/





